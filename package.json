{
  "name": "grand-auto-tech-vercel",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd dashboard && npm install && npm run build && cd .. && rm -rf public && cp -r dashboard/dist public",
    "start:backend": "cd backend && npm start",
    "start:dashboard": "cd dashboard && npm run dev",
    "dev": "npm run start:backend & npm run start:dashboard"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "@electric-sql/pglite": "^0.5.8",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "express-rate-limit": "^6.11.0",
    "express-validator": "^7.0.1",
    "helmet": "^7.0.0",
    "jsonwebtoken": "^9.0.2",
    "morgan": "^1.10.0",
    "pg": "^8.11.3",
    "winston": "^3.11.0"
  }
}
